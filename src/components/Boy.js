import { memo, useMemo, useEffect } from "react"

const Boy = ({ handleBoy, boy }) => {
  console.log(boy)
  return (
    <div>
      {/* <p>hitung boy:</p> */}
      {/* <button onClick={handleBoy}>change boy</button> */}
    </div>
  )
}

export default Boy
